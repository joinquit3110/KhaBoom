footer.global-footer
  .footer-left
    for row in config.footer.rows
      .footer-row
        for link, i in row.links
          if i
            span.dot •
          a(href=link.url)= __(link.title)

  .footer-right
    .footer-row= config.footer.copyright
