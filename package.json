{
  "name": "kha-boom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "deploy": "netlify deploy --prod",
    "predeploy": "npm run build"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
